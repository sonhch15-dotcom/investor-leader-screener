name: Deploy Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Refresh screener data
        run: node src/refresh.mjs

      - name: Run monthly selection test
        run: node src/monthly-selection-test.mjs

      - name: Run full universe group test
        run: node src/full-universe-group-test.mjs

      - name: Run monthly buy rule test 3Y
        run: node src/monthly-buy-rule-test.mjs

      - name: Run monthly buy rule test 5Y
        run: node src/monthly-buy-rule-test.mjs --years 5 --output-suffix -5y

      - name: Build strategy dashboard data
        run: node src/strategy-dashboard-data.mjs

      - name: Build static site
        run: node scripts/build-pages.mjs

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
